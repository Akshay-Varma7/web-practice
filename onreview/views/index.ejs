<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%= m.name %> reviews :</h1>
    <% for(post of posts){ %>
        <div class="post"><!--whenever html comes it gets displayed not erase like function scope-->
            <h3 class="user">@ <%=post.username%></h3>
            <h4 class="content"> <%=post.content%></h4>
            <a href="http://localhost:8080/posts/<%=m.name%>/<%=post.id%>">See in detail</a>
            <a href="http://localhost:8080/posts/<%=m.name%>/<%=post.id%>/edit">Edit</a>
            <form method="post" action="/posts/<%=m.name%>/<%=post.id%>?_method=DELETE">
                <button>Delete Post</button>
            </form>
            
        </div><!--/:id for backend-->
    <%}%>
    <br>
    <br>
    <a href="http://localhost:8080/posts/<%=m.name%>/new">create a post</a><!--http works but https dosent-->
</body>
</html>